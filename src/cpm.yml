prereqs:
  runtime:
    requires:
      File::Temp:
      File::Which:
      File::pushd:
      GitHub::Release: { git: 'https://github.com/skaji/perl-github-release' }
      HTTP::Tinyish:
      IPC::Run3:
      JSON::PP: { version: '4.16' }
      Parallel::Pipes::App: { version: '0.102' }
      YAML::PP:
